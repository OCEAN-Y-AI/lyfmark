/**
 * Astro content layer configuration.
 * This project does not use typed content collections yet; an explicit empty
 * config keeps the content layer in a loaded state and avoids startup warnings.
 */
export const collections = {}
